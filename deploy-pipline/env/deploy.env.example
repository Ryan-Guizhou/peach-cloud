# This is the only local/private file operators need to maintain.
# Existing Jenkins credentials based on the previous full deploy.env remain compatible:
# any extra values in the secret file override repository defaults.
#
# PEACH_RUNTIME_ROOT / PEACH_LOG_ROOT must be paths visible to the Docker daemon.
#
# Windows + Docker Desktop example:
#   Project: D:\Coding\mine\new-mine\peach-cloud
#   PEACH_RUNTIME_ROOT=/host_mnt/d/Coding/mine/new-mine/peach-cloud/deploy-pipline/runtime
#   PEACH_LOG_ROOT=/host_mnt/d/Coding/mine/new-mine/peach-cloud/deploy-pipline/runtime/logs
#
# Linux example:
#   Project: /opt/peach-cloud
#   PEACH_RUNTIME_ROOT=/opt/peach-cloud/deploy-pipline/runtime
#   PEACH_LOG_ROOT=/opt/peach-cloud/deploy-pipline/runtime/logs
#
# Do not use a Windows path such as D:\... as a Docker bind-mount path in this file.
PEACH_RUNTIME_ROOT=/host_mnt/d/path/to/peach-cloud/deploy-pipline/runtime
PEACH_LOG_ROOT=/host_mnt/d/path/to/peach-cloud/deploy-pipline/runtime/logs

MYSQL_ROOT_PASSWORD=change_me_mysql_root_password
REDIS_PASSWORD=change_me_redis_password
MONGO_ROOT_PASSWORD=change_me_mongo_root_password
MONGO_APP_PASSWORD=change_me_mongo_app_password

MAVEN_NEXUS_USERNAME=Development
MAVEN_NEXUS_PASSWORD=change_me_nexus_password
GRAFANA_ADMIN_PASSWORD=change_me_grafana_admin_password

# Local storage is the safe default. Set PEACH_STORAGE_PRIMARY to a cloud provider
# only after the matching access key and secret key are both configured.
# Supported values: local, aliyun, cos, bos, obs.
# PEACH_STORAGE_PRIMARY=local

# Optional external storage credentials used when rendering Nacos configuration.
# A provider is omitted entirely unless both values in its pair are configured.
OSS_ACCESS_KEY=
OSS_SECRET_KEY=
COS_ACCESS_KEY=
COS_SECRET_KEY=
BOS_ACCESS_KEY=
BOS_SECRET_KEY=
OBS_ACCESS_KEY=
OBS_SECRET_KEY=

# Optional only when Nacos auth is enabled or local defaults differ.
# NACOS_AUTH_ENABLE=true
# NACOS_USERNAME=nacos
# NACOS_PASSWORD=change_me_nacos_password

# Optional Maven proxy overrides.
# MAVEN_PROXY_HOST=proxy.example.com
# MAVEN_PROXY_PORT=8080
# MAVEN_PROXY_PROTOCOL=http
# MAVEN_PROXY_ID=corp-proxy
# MAVEN_PROXY_ACTIVE=true
# MAVEN_PROXY_USERNAME=proxy_user
# MAVEN_PROXY_PASSWORD=change_me_proxy_password
