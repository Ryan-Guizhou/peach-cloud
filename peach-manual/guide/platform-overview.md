# 平台功能概览

Peach Cloud 控制台采用 **后端驱动菜单与路由**：你能看到什么，取决于登录后返回的权限快照（菜单、路由、按钮资源、数据权限）。

左侧导航按以下业务域分组（与 `peach-cloud-front` 中菜单分组规则一致）：

## 工作空间

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 工作台 | `/workspace` | 数据总览、快捷入口 |
| 个人中心 | `/profile` | 资料、头像、联系方式 |
| 消息中心 | `/message-center` | 站内消息查看与处理 |

详见 [工作台与消息](/guide/workspace)。

## 身份与权限

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 机构管理 | `/organization` | 组织树与机构信息 |
| 用户管理 | `/user` | 账号、关联机构 |
| 角色管理 | `/role` | 角色定义 |
| 角色授权 | `/authorization` | 菜单 / 资源 / 数据权限授权 |
| 功能定义字段 | `/function` | 功能与字段元数据 |
| 资源管理 | `/resource` | 按钮、接口等资源码 |
| 菜单管理 | `/menu` | 菜单结构维护 |
| 路由管理 | `/router` | 前端路由与组件映射 |

配置顺序与细节见 [授权与数据权限](/guide/authorization)。

## 文件与存储

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 对象浏览 | `/object-browser` | 按存储实例浏览对象 |
| 文件管理 | `/file-record` | 上传文件记录查询 |
| 存储实例 | `/storage-instance` | 存储 Provider 实例配置 |

详见 [文件与存储](/guide/file-storage)。

## 系统设置

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 字典管理 | `/dict` | 系统字典项 |
| 值集管理 | `/value-set` | 业务值集 |
| 多语言管理 | `/multi-message` | 多语言文案 |
| 公告管理 | `/notice` | 系统公告 |
| IP 白名单 | `/ip-whitelist` | 访问 IP 限制 |

## 运行与监控

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 运行监控 | `/runtime-monitor` | 运行时指标与状态 |

## 中间件与审计

| 功能 | 典型路径 | 说明 |
| --- | --- | --- |
| 授权日志 | `/auth-log` | 授权变更审计 |
| 操作日志 | `/operation-log` | 用户操作审计 |

详见 [日志与审计](/guide/logs-and-audit)。

## 权限如何生效

```text
登录 / 切换机构
    ↓
后端装配 LoginPermissionSnapshot
    ↓
前端写入 menuList / routerList / resourceList / dataScopeList
    ↓
动态注册路由 + 渲染菜单 + 按钮显隐 + 列表数据范围
```

::: tip 重要
若后端已授权但前端仍看不到菜单，优先让用户 **切换机构或重新登录**，再查授权日志。
:::

## 与手册站点的关系

- 控制台右上角 **用户手册** → 新标签打开 `/manual/`
- 手册内容与上述模块逐步对齐；尚未覆盖的模块见仓库 `peach-manual/README.md` 待办清单
