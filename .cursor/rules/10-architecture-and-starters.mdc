---
description: 约束模块依赖、公共契约、分包抽象、starter 装配和生成器验收。
globs: "**/*.{java,xml,ftl,vm}"
alwaysApply: false
---
# Architecture And Starters

新增模块、POM 依赖、公共模型/客户端契约、starter/SPI、基础实体或生成器模板时读取。只约束当前任务，不承载长期规划。

## Dependency Boundaries

- 新依赖放到实际使用它的合理模块，说明直接用途和传递影响；BOM 中存在版本不等于模块引入或能力启用。
- 新公共契约只含必要请求、响应和事件，不依赖 Service、DAO、Web/数据库 starter 或厂商 SDK。存量 external→entity 迁移单独评估，不因维护 DTO 自动拆 api jar。
- 模型需要校验/文档注解时依赖 API/annotation，运行时实现由装配层提供。
- common 只承载稳定真实复用；禁止通过反射类名、静态查 Bean、全局服务定位器绕过对具体业务/安全实现的依赖边界。已有桥接可兼容保留，不为新能力复制。
- 基础实体不新增远程调用、缓存、容器查找或具体身份实现绑定；上下文解析和审计填充使用显式依赖。
- 保留既有技术栈；增加 JSON、缓存、重试、映射库前确认现有能力不足。重复客户端与版本覆盖通过 dependency tree/依赖源码判断，不只看 artifactId。
- 库模块不夹带业务启动类、部署环境或凭据；BOM 管版本，launch 负责运行装配。

## Package And Abstraction

- 新类型放 com.peach.<能力或业务域>.*，不新增宽泛顶层 config/request/service 包。存量包名/artifactId 迁移与局部修复分开。
- 新 Maven 模块须有独立发布、依赖隔离、实现替换或测试隔离等理由；不为追求层数创建 jar。
- 简单 CRUD 保持现有分层；当前流程的小型转换/校验优先私有方法，存在独立职责、稳定复用或真实扩展点时再抽类/接口。
- 保留既有公开 Service 接口及命名；新内部 helper 不机械创建 I* + *Impl，不为每个 helper 创建工厂。
- 固定业务结构使用明确类型，Map 仅用于开放键集合或真实动态边界；不为统一所有业务创建万能 CRUD 引擎。

## Starter Contract

- Peach starter 标准结构为 `*-autoconfigure`、`*-starter`、`*-quickstart` 三层；不再兼容 `*-example`，现有模块必须迁移为 `*-quickstart`。
- `*-autoconfigure` 放自动配置、配置属性、配置元数据、条件装配、默认实现、SPI 装配和受管资源生命周期；不得要求业务扩大扫描到 `com.peach`。
- `*-starter` 只聚合业务接入所需最小依赖；可选厂商 SDK、测试工具、quickstart 依赖和 provider 专属依赖不得无条件传递给所有消费者。
- `*-quickstart` 提供可运行样例、最小配置、覆盖默认 Bean、异常配置和排障演示；不得作为业务生产依赖。
- 自动配置使用 Spring Boot 3.5 支持的 `@AutoConfiguration` 与 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 注册方式。
- 配置属性使用 Spring Boot 配置绑定与 configuration processor 生成 IDE 提示；自动生成信息不足时，在 `META-INF/additional-spring-configuration-metadata.json` 补充描述、默认值、枚举 hints 和废弃项提示。
- 明确 enabled、缺失依赖、缺失配置、自定义 Bean 四种装配结果；不能仅凭 ConditionalOnMissingBean 宣称支持覆盖。
- 可选 SDK 不从轻量 API 无条件传递；optional/provider 拆分后验证缺失 SDK 时其他能力可加载，保留旧接入兼容边界。
- 开启能力且必填配置缺失时，在资源创建前给可定位且脱敏的错误，不静默伪装成功。
- 客户端、线程池和订阅明确创建方、共享关系、关闭方；初始化部分失败释放已创建资源，不重复关闭借用资源。
- 重试、超时、容量、幂等及部分失败语义由源码确认并写入公共文档，防止底层与业务重试无意叠加。
- 内存 Outbox/幂等不能代表生产可靠性；建 Topic、建表、建桶不作为普通接入的隐式外部写入。

## Acceptance

- 公共依赖变化检查消费者与实际传递依赖；公共 API 变化检查旧调用方和兼容行为。
- 装配变化使用已有测试设施，优先 ApplicationContextRunner，验证开关、缺失可选依赖、自定义实现、配置失败；资源变化验证关闭与初始化失败。
- 生成器变化检查模板和隔离临时目录的生成结果，做最小编译/装配，不只检查模板语法，不覆盖已有业务文件。
- 注释或等价整理不机械增加全部测试；按实际变化选择。未执行场景明确报告，示例启动不等于失败语义验证。
