---
description: Route Peach Cloud Java changes to the shared Java engineering skill.
globs: "**/*.java"
alwaysApply: false
---
# Java Skill Adapter

读取并遵循 `.agents/skills/peach-java-engineering/SKILL.md`。

本文件只负责 Cursor 的 globs 路由，不复制 Java 规范正文。复杂领域再按根目录 `AGENTS.md` 叠加一个对应 Domain Skill。
