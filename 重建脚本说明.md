# 重建服务脚本使用说明

## 脚本概述

本目录提供了批量重建Peach Cloud所有微服务的脚本，包括：

- `rebuild-all-services.bat` - Windows批处理脚本
- `rebuild-all-services.sh` - Linux/Mac Shell脚本

这些脚本是参考`rebuild-peach-auth.bat`编写的，可一次性重建所有微服务。

## 重建的服务列表

脚本会依次重建以下服务：

- **peach-gateway** - 网关服务
- **peach-auth** - 认证服务
- **peach-monitor** - 监控服务
- **peach-fileservice** - 文件服务
- **peach-message** - 消息服务
- **peach-setting** - 设置服务

注意：脚本不会重建基础设施服务（MySQL、Redis、Nacos），因为这些通常是共享的基础服务。

## 使用方法

### Windows系统
双击运行 `rebuild-all-services.bat` 或在命令行中执行：
```cmd
rebuild-all-services.bat
```

### Linux/Mac系统
在终端中执行：
```bash
./rebuild-all-services.sh
```

## 脚本执行步骤

对于每个服务，脚本会执行以下步骤：

1. **停止服务**: `docker-compose stop <service>`
2. **删除旧镜像**: `docker rmi -f <image>`
3. **构建新镜像**: 在相应服务目录下执行 `docker build`
4. **启动服务**: `docker-compose up -d <service>`

## 使用场景

- 当您修改了某个服务的代码后，需要重新构建Docker镜像
- 当您需要更新所有服务到最新版本时
- 当您遇到服务异常，需要重新部署时

## 注意事项

- 请确保Docker和Docker Compose已安装并正常运行
- 请确保相应的服务目录和Dockerfile存在
- 脚本执行期间会停止相关服务，可能会影响系统可用性
- 重建过程可能需要一些时间，请耐心等待
- 如果只想重建特定服务，仍可使用单独的 `rebuild-peach-auth.bat` 等脚本

## 错误处理

- 如果某个服务构建失败，脚本会显示错误信息并退出
- 如果服务原本没有运行，脚本会显示警告但继续执行
- 如果镜像不存在，脚本会显示警告但继续执行
- 修复了原始脚本中可能导致闪退的标签跳转问题
- 在Windows脚本中使用 `call :process_service` 和 `setlocal/endlocal` 确保变量处理正确